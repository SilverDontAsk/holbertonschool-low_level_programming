#include "main.h"
/**
 * mul - multiplies two integers
 * @a: first interger to be multiplied with
 * @b: second interger to be multiplied with
 * Return: result of multiplication
 */
int mul(int a, int b)
{
return (a * b);
}
