#include <stdio.h>

/**
 * main - This would be the entry point of the program
 *
 * Description: Basically print a message by using puts instead of printf
 *
 * Return: Always 0 (0=sucess)
 */
	int main(void) 
	{
		/**
		 * Puts is another way to print the message
		 */
		puts("\"Programming is like building a multilingual puzzle");
		
		return (0);
}
