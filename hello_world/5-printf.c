#include <stdio.h>

/**
 * main - Basically just the entry point of the whole program
 *
 * Description: Use printf to output a message this time instead of puts.
 *
 * Return: Should always be 0 (Where 0 means it was successful)
 */
int main(void)
{
	/**
	 * Using printf to print the message followed by a new line
	 *
	 */
	printf("with proper grammar, but the outcome is a piece of art,\n");

	return (0);
}
