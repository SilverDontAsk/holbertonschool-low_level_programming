#include "main.h"
/**
 * print_numbers - print 0 to 9 followed by new line
 * 
 * Return: always 0
 */
void print_numbers(void)
{
int ch = 0;
for (ch = 0; ch <= 9; ch++)
{
_putchar(ch + '0');
}
{
_putchar('\n');
}
}
