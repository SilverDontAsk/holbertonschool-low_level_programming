#include "main.h"

/**
 * main - print out the alphabet
 *
 * Return: Always 0
 */
void print_alphabet(void);
{
char ch = 'a';
for(ch = 'a'; ch <= 'z'; a++)
_putchar(ch);
_putchar('\n');
return (0);
}
