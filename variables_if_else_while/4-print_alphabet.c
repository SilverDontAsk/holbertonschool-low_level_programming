#include <stdio.h>
/**
 * main - print alphabet in lowercase without q or e
 *
 * Return: Always 0
 */
int main(void)
{
char = 'a';

for (ch = 'a'; ch <= 'z'; ch++)
{
if(ch != 'q' && ch != 'e')
}
{
putchar(ch);
}
putchar('\n');
return (0);
}
